import { RolesScreen } from '@/features/settings/components/roles/roles-screen';

/** The role editor renders its own "Workspace Permissions" panel header (per the design). */
export default function RolesSettingsPage() {
  return <RolesScreen />;
}
