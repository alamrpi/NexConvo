{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "_comment": "Local dev: owner user bypasses RLS — fine for dev. Run migrations first: dotnet ef database update --project ../NexConvo.Chat.Infrastructure --startup-project .",
    "ChatDb": "Host=localhost;Port=5432;Database=nexconvo_chat;Username=nexconvo;Password=localdev_pg_pw"
  }
}
