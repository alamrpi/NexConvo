{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "_comment": "Local dev: owner user bypasses RLS — fine for dev. Run migrations first: dotnet ef database update --project ../NexConvo.Chat.Infrastructure --startup-project . Redis backs both the distributed cache and the SignalR backplane (compose starts redis with this password).",
    "ChatDb": "Host=localhost;Port=5432;Database=nexconvo_chat;Username=nexconvo;Password=localdev_pg_pw",
    "Redis": "localhost:6379,password=localdev_redis_pw"
  },
  "Internal": {
    "_comment": "Local non-Docker dev default only — env Internal__ApiKey overrides this in compose/prod. Same value as Knowledge's Internal:ApiKey (Standard 13 — shared secret).",
    "ApiKey": "localdev_internal_key"
  }
}
