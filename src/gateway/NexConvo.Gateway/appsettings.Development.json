{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "_comment": "Local dev override: services run via `dotnet run` on localhost, not k8s DNS. Only the Identity service is required for the auth flow; CRM/Chat keep their base (k8s) addresses and are simply unrouted until run locally.",
  "Jwt": {
    "Authority": "http://localhost:5047"
  },
  "ReverseProxy": {
    "Clusters": {
      "identity-cluster": {
        "Destinations": {
          "primary": { "Address": "http://localhost:5047" }
        }
      },
      "integrations-cluster": {
        "Destinations": {
          "primary": { "Address": "http://localhost:5018" }
        }
      },
      "chat-cluster": {
        "Destinations": {
          "primary": { "Address": "http://localhost:5132" }
        }
      }
    }
  }
}
